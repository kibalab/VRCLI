# VRCLI Unity Bridge

This editor-only VPM package is the Unity-side bridge for the VRCLI executable. VRCLI bundles and installs it automatically when it is absent from a target project.

The package is not intended to be invoked directly. See the repository root README for CLI installation, authentication, and CI examples.

